# Render Blueprint - Samsa Prediction Markets
# Deploy at: https://render.com/deploy

services:
  - type: web
    name: samsa
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001

